apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "esrd-web.fullname" . }}-test-connection"
  labels:
    {{- include "esrd-web.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "esrd-web.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
