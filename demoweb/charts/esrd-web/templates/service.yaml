apiVersion: v1
kind: Service
metadata:
  name: {{ include "esrd-web.fullname" . }}
  labels:
    {{- include "esrd-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "esrd-web.selectorLabels" . | nindent 4 }}
